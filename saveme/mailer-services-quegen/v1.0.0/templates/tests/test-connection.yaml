apiVersion: v1
kind: Pod
metadata:
  labels:
    {{- include "mailer-services-quegen.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mailer-services-quegen.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
