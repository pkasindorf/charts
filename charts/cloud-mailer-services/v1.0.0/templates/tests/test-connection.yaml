apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cloud-mailer-services.fullname" . }}-test-connection"
  labels:
    {{- include "cloud-mailer-services.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cloud-mailer-services.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
