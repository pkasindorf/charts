apiVersion: v1
kind: Service
metadata:
  name: {{ include "digest-q-gen.fullname" . }}
  labels:
    {{- include "digest-q-gen.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "digest-q-gen.selectorLabels" . | nindent 4 }}
