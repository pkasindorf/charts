apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mailer-services-redux.fullname" . }}-test-connection"
  labels:
    {{- include "mailer-services-redux.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mailer-services-redux.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
