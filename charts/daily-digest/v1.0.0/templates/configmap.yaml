apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "daily-digest.fullname" . }}-config
  labels:
    app: {{ template "daily-digest.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
data:
  config.yml: |-
{{ toYaml .Values.configData | indent 4 }}
