apiVersion: v1
kind: Service
metadata:
  name: {{ include "digest-mailer.fullname" . }}
  labels:
    {{- include "digest-mailer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "digest-mailer.selectorLabels" . | nindent 4 }}
