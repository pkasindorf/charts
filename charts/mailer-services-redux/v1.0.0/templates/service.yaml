apiVersion: v1
kind: Service
metadata:
  name: {{ include "mailer-services-redux.fullname" . }}
  labels:
    {{- include "mailer-services-redux.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mailer-services-redux.selectorLabels" . | nindent 4 }}
