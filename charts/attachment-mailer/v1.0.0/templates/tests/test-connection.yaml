apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attachment-mailer.fullname" . }}-test-connection"
  labels:
    {{- include "attachment-mailer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attachment-mailer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
