apiVersion: v1
kind: Service
metadata:
  name: {{ include "attachment-mailer.fullname" . }}
  labels:
    {{- include "attachment-mailer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attachment-mailer.selectorLabels" . | nindent 4 }}
