apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mailer-services-cloudmailer.fullname" . }}-test-connection"
  labels:
    {{- include "mailer-services-cloudmailer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mailer-services-cloudmailer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
