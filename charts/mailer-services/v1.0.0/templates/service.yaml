apiVersion: v1
kind: Service
metadata:
  name: {{ include "mailer-services.fullname" . }}
  labels:
    {{- include "mailer-services.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mailer-services.selectorLabels" . | nindent 4 }}
