apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pulp-fiction.fullname" . }}-test-connection"
  labels:
    {{- include "pulp-fiction.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pulp-fiction.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
