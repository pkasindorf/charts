apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fitness-center-weekly.fullname" . }}-test-connection"
  labels:
    {{- include "fitness-center-weekly.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fitness-center-weekly.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
