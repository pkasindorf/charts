apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mailer-services-dailydigest.fullname" . }}-test-connection"
  labels:
    {{- include "mailer-services-dailydigest.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mailer-services-dailydigest.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
