apiVersion: v1
kind: Service
metadata:
  name: {{ include "fitness-center-weekly.fullname" . }}
  labels:
    {{- include "fitness-center-weekly.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fitness-center-weekly.selectorLabels" . | nindent 4 }}
