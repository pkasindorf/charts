apiVersion: v1
kind: Service
metadata:
  name: {{ include "mailer-services-cloudmailera.fullname" . }}
  labels:
    {{- include "mailer-services-cloudmailera.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mailer-services-cloudmailera.selectorLabels" . | nindent 4 }}
