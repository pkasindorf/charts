apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "digest-q-gen.fullname" . }}-test-connection"
  labels:
    {{- include "digest-q-gen.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "digest-q-gen.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
