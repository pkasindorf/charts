apiVersion: v1
kind: Service
metadata:
  name: {{ include "priority-mailer.fullname" . }}
  labels:
    {{- include "priority-mailer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "priority-mailer.selectorLabels" . | nindent 4 }}
