apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "priority-mailer.fullname" . }}-test-connection"
  labels:
    {{- include "priority-mailer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "priority-mailer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
