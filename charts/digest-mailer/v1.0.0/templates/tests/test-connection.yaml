apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "digest-mailer.fullname" . }}-test-connection"
  labels:
    {{- include "digest-mailer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "digest-mailer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
