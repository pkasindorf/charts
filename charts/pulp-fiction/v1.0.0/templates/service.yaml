apiVersion: v1
kind: Service
metadata:
  name: {{ include "pulp-fiction.fullname" . }}
  labels:
    {{- include "pulp-fiction.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pulp-fiction.selectorLabels" . | nindent 4 }}
