apiVersion: v1
kind: Service
metadata:
  name: {{ include "mailer-services-cloudmailer.fullname" . }}
  labels:
    {{- include "mailer-services-cloudmailer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mailer-services-cloudmailer.selectorLabels" . | nindent 4 }}
