apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloud-mailer-services.fullname" . }}
  labels:
    {{- include "cloud-mailer-services.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloud-mailer-services.selectorLabels" . | nindent 4 }}
